1. The bot currently runs in a Cloudflare Worker, which is a lightweight environment that does not support image editing libraries (like Canvas) to draw lines onto a PNG. 2. Even if we added an image library, the bot doesn't know the exact (X, Y) pixel coordinates of all the rooms on the chsmap.png image to connect them. Here is what I recommend we build for now: 1. I will create a /setupclasses command where users can input their 1st, 2nd, 3rd, 4A, 4B, 5th, 6th period room numbers, and their lunch shift. 2. I will create the /mapmyclass [day] command (where day is A day or B day, though the bot can also automatically detect if today is an A/B day). 3. When they run /mapmyclass, the bot will output a cleanly formatted embed that lists their route step-by-step for that specific day (e.g., Room 101 ➔ Room 205 ➔ Lunch ➔ Room 302). 4. The bot will attach the chsmap.png image directly to the embed so they can reference the map right there in Discord. If you really want the drawn lines in the future, we could host an interactive web page on the bot where users click a link to view their route drawn in their browser, but I would first need you to manually map out the X/Y coordinates of every room on the map. Does the text-based route with the attached map image sound like a good plan for now? ──────────────────────────────────────────────────────────── >
BUT i want it to draw over the map with a red line instead